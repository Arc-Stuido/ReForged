package net.neoforged.neoforge.common.util;

import net.minecraftforge.fml.LogicalSide;

import java.util.function.Supplier;

/**
 * Stub: Provides access to logical side-specific objects (e.g. current server).
 */
public class LogicalSidedProvider {

    @SuppressWarnings("unchecked")
    public static <T> T get(LogicalSide side) {
        throw new UnsupportedOperationException("LogicalSidedProvider.get not implemented in ReForged stub");
    }
}
